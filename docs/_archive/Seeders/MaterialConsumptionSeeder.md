# MaterialConsumptionSeeder

**Файл:** `database/seeders/MaterialConsumptionSeeder.php`

## Назначение сида

Создание записей о расходе материалов для производства товаров.

## Создаваемые данные

**Количество:** 0 записей (сеeder пустой)

## Особенности генерации

- Сеeder не содержит логики создания данных
- Предположительно, расход материалов создается в других сеедерах (например, в
  `MarketplaceItemSeeder`)

## Зависимости от других сидов

Не имеет зависимостей.

## Выполняемые операции

Нет операций - метод `run()` пустой.

## Примечания

Расход материалов фактически создается в `MarketplaceItemSeeder`, где для
каждого товара создается соответствующая запись о расходе материалов.
